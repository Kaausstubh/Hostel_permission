# Production environment — used by Vercel on deployment
# This file is safe to commit — it contains only public API URLs, no secrets.
VITE_API_URL=https://hostel-permission-8add.onrender.com/api
VITE_API_TIMEOUT_MS=15000
